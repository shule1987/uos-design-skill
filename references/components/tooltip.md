---
inclusion: manual
---

# 提示组件

## Tooltip
- 用途：用于补充说明图标含义、缩写、截断文本或低频操作的简短解释。
- 结构：一般只承载一小段文本，不放复杂交互。
- 行为：由 hover 或焦点触发，离开目标后自动消失；不应遮挡主要操作区域太久。

## Toast
- 用途：用于轻量、短时的操作结果反馈。
- 结构：通常包含一段消息和可选状态图标，必要时允许一个轻量操作。
- 行为：自动出现并在短时间后消失；重要错误或需要确认的内容不应使用 Toast 承载。
