---
inclusion: manual
---

# 侧边栏组件

## Sidebar
> 示例描述：这里定义 `Sidebar` 组件，基于 `Rectangle` 实现，对外暴露 `collapsed`、`collapsible`、`blurEnabled` 和 `items` 等属性。 尺寸与样式上宽度使用 `collapsed ? 60 : 240`；结构上使用 `WindowBlur`、`Column` 和 `IconButton` 组织内容；交互上覆盖悬停反馈、点击触发、键盘回车和空格操作、无障碍语义、过渡动画。
