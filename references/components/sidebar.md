---
inclusion: manual
---

# 侧边栏组件

## Sidebar
- 用途：用于一级导航、资源分组、筛选入口和少量全局操作。
- 结构：通常由顶部品牌区或标题区、导航项列表、可选的底部收起按钮组成。
- 尺寸与状态：展开宽度通常在 `200-240px`，折叠后可收缩到 `60px` 左右；折叠态保留图标、当前选中态和必要 tooltip。
- 视觉：背景优先使用 `Theme.bgPanel`；在性能允许且平台支持时可增加轻度 blur，但必须有纯色回退。
- 交互：支持 hover、选中、键盘导航和无障碍名称；收起与展开应使用短时过渡，不影响主要操作流。
