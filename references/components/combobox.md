---
inclusion: manual
---

# 下拉选择组件

## ComboBox
- 用途：用于从有限选项中选择单个值，适合表单、筛选器和设置项。
- 结构：闭合态显示当前值和下拉指示图标；展开态显示选项列表，必要时支持图标、分组或辅助说明。
- 尺寸与样式：高度应与输入框、按钮等基础控件保持同一密度；圆角使用 `Theme.radiusSm`。
- 交互：支持点击展开、方向键切换、`Enter` 确认、`Esc` 关闭，并在关闭后把焦点返回到触发控件。
