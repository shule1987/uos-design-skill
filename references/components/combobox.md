---
inclusion: manual
---

# 下拉选择组件

## ComboBox
> 示例描述：这里定义 `ComboBox` 组件，基于 `Rectangle` 实现，对外暴露 `model`、`currentIndex` 和 `accessibleName` 等属性。 尺寸与样式上宽度使用 `200`，高度使用 `36`，圆角使用 `Theme.radiusSm`；结构上使用 `Row` 和 `Popup` 组织内容；交互上覆盖悬停反馈、点击触发、键盘回车和空格操作、无障碍语义。
