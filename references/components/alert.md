---
inclusion: manual
---

# 警告提示组件

## Alert
- 用途：用于页面内常驻的状态提示、风险说明和操作提醒。
- 结构：通常包含状态图标、标题、正文和可选关闭按钮。
- 视觉：根据 `success`、`warning`、`danger` 等语义选择对应强调色，但仍以可读文本为主。
- 交互：关闭后是否永久隐藏要按业务场景决定；关键提示不应轻易允许用户误关后完全失联。
